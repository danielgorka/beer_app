import 'package:flutter/material.dart';

const beersGridViewDelegate = SliverGridDelegateWithMaxCrossAxisExtent(
  maxCrossAxisExtent: 200,
  mainAxisSpacing: 8,
  mainAxisExtent: 325,
);

const beersViewPadding = EdgeInsets.all(16);
